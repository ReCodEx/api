{emailSubject}Review comment removed{/emailSubject}

<h3>{$comment->isIssue() ? 'An issue' : 'A comment'} was removed from a solution review!</h3>

<p>
    <strong>{$comment->isIssue() ? 'An issue' : 'A comment'} was removed</strong> from the review of solution
    <strong>#{$attempt}</strong> (submitted at <strong>{$submitted|localizedDate:'en'}</strong>)
    of the assignment <strong>{$assignment}</strong> in the group <strong>{$group}</strong>
    that was closed by the teacher at <strong>{$closed|localizedDate:'en'}</strong>.
</p>

<div>
    <h5 n:if="!$comment->getFile()">Removed from <code>{$comment->getFile()}:{$comment->getLine()}</code> ...</h5>
    <h5 n:if="!$comment->getFile()">Review summary removed</h5>
    {$comment->getText()|markdown}
</div>

<p>
    The complete review is available at: <a href="{$codeUrl}">{$codeUrl}</a>
</p>
